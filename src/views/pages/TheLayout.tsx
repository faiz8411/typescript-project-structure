import React from "react";
import { Header } from "./common/Header";

const TheLayout = () => {
  return (
    <div>
      <Header />
      <button>search</button>
    </div>
  );
};

export default TheLayout;
